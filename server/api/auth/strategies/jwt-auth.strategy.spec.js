describe('JWT Auth Strategy', function () {
    it('should populate the request object with user on successful token validation');
    it('should return an error when token is not provided');
    it('should return an error when secret is not provided');
    it('should return an error when user is not found');
    it('should return an error on any other technical errors');
});
